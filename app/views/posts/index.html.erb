<h1>
	All posts
</h1>

<%= form_for @post do |t| %>
	<p>
		<%= t.label :title %>
		<%= t.text_field :title %>
	</p>

	<p>
		<%= t.label :post %>
		<%= t.text_field :p_content %>
	</p>

	<p>
		<%= t.submit %>
	</p>
<% end %>

<% @posts.reverse_each do |post| %>
	<h2>
		<%= post.title %>
	</h2>

	<p>
		<%= post.p_content %>
	</p>

	<ul>
		<li>
			<%= post.user.username %>
		</li>
		
		<li>
			<%= post.created_at %>
		</li>
	</ul>
<% end %>